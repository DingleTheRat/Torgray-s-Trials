package entity;

public enum EntityTypes {
    // Main
    TYPE_PLAYER,
    TYPE_NPC,
    TYPE_MOB,

    // Objects
    TYPE_OBJECT, // If it's nothing crazy and just an object
    TYPE_SWORD,
    TYPE_SHIELD,
    TYPE_CONSUMABLE
}
